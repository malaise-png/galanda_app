# Install with: pip install -r requirements.txt
#
# On the Raspberry Pi, follow Kivy's official Raspberry Pi install guide
# for `kivy[base]`.

kivy[base]==2.3.1
# Used to pre-generate small on-disk thumbnails for the category picker
# (see image_assets.py) -- without this, opening a category decodes every
# full-resolution artwork PNG on the UI thread, which is what caused the
# picker's noticeable open delay.
pillow
