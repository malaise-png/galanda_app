Drop a single PNG here (a paper/canvas-like texture). It becomes CanvasArea's
own background, stretched to fill the whole canvas, and is visible as a
border around whatever POZADIE image the user picks (see theme.POZADIE_INSET).

There's no flat-color fallback -- if this folder is empty, the canvas is
simply blank until a PNG is added here.
